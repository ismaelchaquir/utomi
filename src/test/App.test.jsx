// import { render } from "@testing-library/react";

describe("jest", () => {
  it("should work", () => {
    expect(1).toBe(1);
  });

  // it("should display element", () => {
  //   render(<App />);

  //   screen.debug();
  // });
});
